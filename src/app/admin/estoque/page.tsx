"use client";
import { useEffect,useState } from "react";
export default function InventoryPage(){const[products,setProducts]=useState<any[]>([]);const[message,setMessage]=useState('');useEffect(()=>{void fetch('/api/admin/products',{cache:'no-store'}).then(async r=>{if(!r.ok)throw new Error('Supabase ainda não configurado.');setProducts((await r.json()).products??[])}).catch(e=>setMessage(e.message))},[]);return <main className="p-4 md:p-6 lg:p-8"><div><p className="text-xs font-black uppercase tracking-wider text-[#A88A45]">Saldo real</p><h1 className="mt-1 text-3xl font-black text-[#1F2A44]">Estoque</h1><p className="mt-1 text-sm text-slate-500">Físico, reservado e disponível.</p></div>{message&&<div className="mt-5 rounded-2xl bg-amber-50 p-4 text-sm font-bold text-amber-900">{message}</div>}<div className="mt-6 overflow-hidden rounded-[2rem] border border-[#E8DCC8] bg-white"><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-[#F4ECDF] text-[10px] uppercase text-slate-500"><tr><th className="p-4">Produto</th><th>Físico</th><th>Reservado</th><th>Disponível</th><th>Mínimo</th><th>Situação</th></tr></thead><tbody>{products.map(p=><tr key={p.id} className="border-t border-[#EFE5D6]"><td className="p-4 font-black text-[#1F2A44]">{p.name}</td><td>{p.onHand??p.stock}</td><td>{p.reserved??0}</td><td className="font-black">{p.stock}</td><td>{p.minimumStock??0}</td><td><span className={`rounded-full px-2.5 py-1 text-[10px] font-black ${p.stock<=p.minimumStock?'bg-amber-100 text-amber-800':'bg-emerald-100 text-emerald-800'}`}>{p.stock<=p.minimumStock?'REPOR':'OK'}</span></td></tr>)}</tbody></table></div></div></main>}
