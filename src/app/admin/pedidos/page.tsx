import { adminOrders } from "@/lib/mock-data";

const columns = ["NOVO", "SEPARANDO", "PRONTO", "EM ENTREGA"];
const brl = new Intl.NumberFormat("pt-BR", { style: "currency", currency: "BRL" });

export default function OrdersPage() {
  return <main className="p-4 md:p-6 lg:p-8"><div><p className="text-xs font-black uppercase tracking-wider text-emerald-600">Fluxo vivo</p><h1 className="mt-1 text-3xl font-black">Central de pedidos</h1><p className="mt-1 text-sm text-slate-500">A operação deve avançar o pedido com o mínimo de cliques possível.</p></div><section className="mt-6 grid gap-4 xl:grid-cols-4">{columns.map((column) => <div key={column} className="min-h-72 rounded-[2rem] bg-slate-200/70 p-3"><div className="flex items-center justify-between px-2 py-2"><h2 className="text-xs font-black tracking-wider">{column}</h2><span className="rounded-full bg-white px-2 py-0.5 text-xs font-black">{adminOrders.filter((o) => o.status === column).length}</span></div><div className="mt-1 space-y-3">{adminOrders.filter((o) => o.status === column).map((order) => <article key={order.id} className="rounded-3xl bg-white p-4 shadow-sm"><div className="flex justify-between"><p className="text-sm font-black">{order.id}</p><span className="text-xs font-bold text-slate-400">{order.time}</span></div><p className="mt-2 truncate text-sm font-semibold">{order.customer}</p><p className="mt-1 text-xs text-slate-400">{order.items} itens • {order.payment}</p><p className="mt-3 text-lg font-black">{brl.format(order.total)}</p><button className="mt-3 w-full rounded-xl bg-slate-950 px-3 py-2.5 text-[11px] font-black text-white">{column === "NOVO" ? "INICIAR SEPARAÇÃO" : column === "SEPARANDO" ? "ABRIR SEPARAÇÃO" : column === "PRONTO" ? "DESPACHAR" : "VER ENTREGA"}</button></article>)}</div></div>)}</section></main>;
}
